stages:
    - deploy
    
job_deploy_master:
    stage: deploy
    only: 
        - master
    script:
        - composer install
        - cp /home/ben/workspace/benbot/env_stuff.php .
        - ./bot start
        